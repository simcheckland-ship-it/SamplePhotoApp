# Inside your nginx.conf
location /uploads/ {
    alias /var/www/photo-app/uploads/; # This matches your container path exactly!
    expires 30d;
    valid_referers none blocked localhost localhost:3000 192.168.0.*;
    if ($invalid_referer) { return 403; }
}
