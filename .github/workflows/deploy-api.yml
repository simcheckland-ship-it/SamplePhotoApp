name: Deploy API Server Changes

on:
  push:
    branches:
      - main
    paths:
      - "api-server/**"
      - ".github/workflows/deploy-api.yml"

jobs:
  deploy:
    runs-on: [self-hosted, linux]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5 # Automatically cleans and checks out the exact commit

      - name: Copy API Server Files to Proxmox
        run: |
          # 1. Clean out the old deployment folder on the server to prevent directory mixing
          ssh -i /home/github-runner/.ssh/runner-to-vm1 -o StrictHostKeyChecking=no "${{ secrets.VM2_USER }}@192.168.0.201" "rm -rf ~/react-photo-app/api-server && mkdir -p ~/react-photo-app/api-server"

          # 2. Copy the actual CONTENTS of your new best-practice code folder directly over the network
          scp -i /home/github-runner/.ssh/runner-to-vm1 -o StrictHostKeyChecking=no -r api/PhotoWepApi/* ${{ secrets.VM2_USER }}@192.168.0.201:~/react-photo-app/api-server/
      - name: Remote Restart via SSH
        run: |
          # Remotely trigger Docker inside the target folder without checking out Git on the server
          ssh -i /home/github-runner/.ssh/runner-to-vm1 -o StrictHostKeyChecking=no "${{ secrets.VM2_USER }}@192.168.0.201" "
            cd ~/react-photo-app/api-server && 
            docker compose down &&
            docker compose up -d --build
          "
