name: "🟢 Deploy/Update Server Set 20 Infrastructure"

on:
  workflow_dispatch:

jobs:
  terraform:
    name: "Run Terraform"
    runs-on: self-hosted

    defaults:
      run:
        working-directory: ./terraform

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Terraform Init
        run: terraform init

      - name: Generate Secure TFVars
        run: |
          cat << 'EOF' > secret.auto.tfvars
          proxmox_endpoint = "${{ secrets.PROXMOX_ENDPOINT }}"
          proxmox_token    = "${{ secrets.PROXMOX_TOKEN }}"
          server_passwords = {
              "VM200" = "${{ secrets.VM0_PASS }}",
              "VM201" = "${{ secrets.VM1_PASS }}",
              "VM201" = "${{ secrets.VM2_PASS }}",
          }
          EOF

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        
      - name: Run Ansible Configuration Playbook
        working-directory: ./ansible  
        run: |
          # 1. Ensure basic SSH configuration directory exists
          mkdir -p ~/.ssh
      
          # 2. Configure Ansible flags globally to silence warnings and force color
          export ANSIBLE_HOST_KEY_CHECKING=False
          export ANSIBLE_FORCE_COLOR=True
          export ANSIBLE_PYTHON_INTERPRETER=auto_silent
      
          # 3. Force OpenSSH to ignore known_hosts entirely for this run
          ansible-playbook -i inventory.ini site.yml \
            --private-key=~/.ssh/runner-vm \
            --ssh-common-args='-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o IdentitiesOnly=yes'
        shell: /usr/bin/bash -e {0}

  #call-child:
#    # Calls another YAML file in the same repository
#    needs: terraform
#    uses: ./.github/workflows/vm20-deploy-proxy.yml
#    secrets: inherit
