---
- name: "🌐 Baseline OS Hardening "
  hosts: all # 🟢 Targets every machine inside inventory.ini
  become: true

  tasks:
    - name: Install baseline system prerequisites
      apt:
        name:
          [
            apt-transport-https,
            ca-certificates,
            curl,
            gnupg,
            software-properties-common,
          ]
        state: present
        update_cache: yes

    - name: Assign deployment user to the docker group
      user:
        name: "{{ ansible_user }}"
        groups: docker
        append: yes
