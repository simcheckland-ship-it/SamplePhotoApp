---
- name: "🔒 Proxy Server Specific Configuration"
  hosts: app_proxy_server # 🎯 ONLY targets your proxy VM node
  become: true

  tasks:
    - name: Create custom Nginx configuration directories
      file:
        path: /etc/nginx/ssl
        state: directory
        mode: "0700"
