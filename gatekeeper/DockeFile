FROM nginx:alpine

# Remove the default configurations
RUN rm /etc/nginx/conf.d/default.conf

# Copies nginx.conf directly from /gatekeeper into the template folder
COPY nginx.conf /etc/nginx/templates/gatekeeper.conf.template

EXPOSE 80
